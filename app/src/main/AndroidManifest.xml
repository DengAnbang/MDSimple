<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.dab.mdsimple">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Base">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".kotlin.activity.fab.FloatingActionButtonActivity">
        </activity>
        <activity android:name=".kotlin.activity.tab.TabLayoutActivity">
        </activity>
        <activity android:name=".kotlin.activity.coordinator.CoordinatorActivity">
        </activity>
    </application>

</manifest>